<section class="section skinny">
  <h1 class="headline">Tags</h1>
    <article>
        <div class="page-nav">
          <% @tags.each do |tag| %>
            <a class="blog__excerpt" href="/tags/<%= tag.name %>">
              <h2 class="tag-meta"><%= tag.name %></h2>
            </a>
          <% end %>
        </div>
    </article>
</section>

